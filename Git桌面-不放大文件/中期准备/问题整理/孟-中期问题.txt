问1：时间序列预测主要是用到哪些场景？

问2：有这些数据，对缺失值进行填补，缺失值有没有限定，比如占比，不可能占百分之80时都填补？

问3：房价数据集有哪些缺失？

问4：房价并不是按天，按周也可以?

问5：缺失值的填补，填补的是一个假的数据，只是让数据更平滑，对吗？

问6：如何验证填充的这个值对你更有用，做这个事情最核心的动机是什么？

问7：为什么不针对非等间隔数据直接用非等间隔模型?

问8：序列预测和分类方法中存在数据缺失很正常，对数据缺失进行填补，填充的数据通过模型有可能是引入了噪声，能不能专门针对这个数据集，不改变数据集的前提下设计方法？

问9：为什么要先填充数据，不直接对未填充的数据进行研究？
