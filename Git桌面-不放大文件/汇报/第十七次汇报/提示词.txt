
**优化后的问题**：

我是软件工程专业的研究生，正在撰写一篇实验论文，研究不同超参数对机器学习模型性能的影响。我已经完成实验，并将实验结果存储在一个 CSV 文件中，并读取为 Pandas DataFrame（`df_census`）。DataFrame 的列如下：

- **超参数列（自变量）**：
  - `base_estimator`（基础模型）
  - `correlation_threshold`（相关性阈值）
  - `miss_rate`（缺失率）

- **性能指标列（因变量）**：
  - `RMSE`（均方根误差）
  - `MSE`（均方误差）
  - `MAE`（平均绝对误差）
  - `R²`（决定系数）
  - `Accuracy`（准确率）
  - `Precision`（精确率）
  - `Recall`（召回率）
  - `F1-score`（F1 分数）
  - `elapsed_time`（运行时间）

### **问题**
基于 `df_census` 数据，请你设计实验数据分析方案，包括：
1. **Excel 数据表设计**：
   - 可以构建哪些 Excel 表格？
   - 每个表格的目的是什么？
   - 每个表格的行列应该如何组织？

2. **数据可视化**：
   - 可以绘制哪些图表？
   - 每个图表的目的是什么（希望揭示什么关系）？
   - 如何绘制这些图（X 轴、Y 轴、图例等的选择）？

请详细说明你的分析方法，并提供合理的实验设计方案。

要求：提供的方案文档要科学正确、学术严谨、详细，我可以根据这个方案取分析我的数据并撰写论文