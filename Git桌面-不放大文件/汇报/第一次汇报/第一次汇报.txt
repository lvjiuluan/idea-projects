1、相关性不大，如何来生成。确定用哪些列生成，生成几个。直接在B方法做生成，哪些做生成，哪些做填补，生成是生成几个.
没有相关性B方中的一些列做生成，剩下做填补
2、相关性大的时候，如何来生成，充分去论证。B选择哪些列，怎么选择
设置一套规则
生成的属性必须要满足一定条件的。
通过大量的实验来生成大量属性，生成一个或者两个。
这些需要通过实验来验证。

但这些始终，要比单方做好。
有一定相关性的，一对多的情况，多对1，多对多。
多对1，按照聚类、或者概率来生成。半监督



还得确定算像关系，只看一个还是看全部。

A5 B5

分类问题

回归问题

A当作的某一列在B当作可能对于多个。



不相关:随机生成。涉及到是不是A有数据，B方
首先：有没有这种方法，A方没有，B方有
如果有，就把这个作为baseline
如果没有：baseline：A方直接生成。 GAN网络
根据B的数据填补A的数据。
选一个变量来跑，还是选两个变量来跑
要讨论一下。

想一想有什么可以结合的。




相关：1对多和多对1的情况。

