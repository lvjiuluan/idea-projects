相关性的计算方法

A方的某一列还是A方的所有列跟B方算相关性
到底生成多少个列是有效的，再去填补。

接下来

![image-20240419142128520](D:/Typora/images/image-20240419142128520.png)

找多对多的规则，不相关的情况的规则。或多或少带一点关系。

某一个列直接跟另外一个列必定是一对一关系的。



B方自己去生成k到j

跟A有关系的生成，就自己跟A的5列去生成。

B不知道，CTGAN是

这种方式的合理性

![image-20240419142850522](D:/Typora/images/image-20240419142850522.png)

关联规则的算法有很多中

CTGAN没有做GAN网络的生成，只用了关联规则。

接下来试一下不同的关联规则。

只是一列跟一列

A方的所有列来对B方一个列。

1对1

来对某一个列生成一个列、两个列、

1对1的接下来要去尝试不同的关联规则。

m个列对1个列去测试。

生成5轮就是5个列



1、不同的关联规则

2、产生用m个生成这些列。

3、用联邦的方式做CTGAN

4、其它数据集，AB方不同划分情况。

有B的m列一次性生成m个列来，一个列一个列的生成。也可以产生生成多个列的效果。

A方的一个列生成生成B，直接用gan网络的方式去做。

![image-20240419144609772](D:/Typora/images/image-20240419144609772.png)

其它关联规则的方法